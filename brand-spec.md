# 真实标签 PRD 品牌与界面规范

> 当前视觉事实来源是 `产品需求文档_PRD.md` 第 5 章；运行时由 `app/static/css/prd-contract.css` 最后加载并收口。本文中的分层 CSS 仍用于布局，但任何旧 V2 数值与 PRD 冲突时，以 PRD 为准。

## 1. 视觉结论

视觉语言是「可信关系实验室」：把真实数据、匿名边界与关系进度做成一张可以操作的编辑式实验板。界面使用玩具箱式新粗野主义秩序，但保留真实标签自身的产品名称、业务内容和隐私语义。

参考视觉位于 `docs/design-reference-brutalist.png`。它只作为视觉方向，不是页面内容或产品指令。

## 2. 核心原则

1. 先读层级，再感受个性。标题、数据、行动和安全说明必须在三秒内分层。
2. 黑色边界代表规则。粗边框不是装饰，而是内容、状态与行动的明确分区。
3. 高饱和色有语义。黄色用于引导与事实，紫色用于品牌与主要推进，绿色用于成功和已验证，珊瑚色用于风险与提醒。
4. 动画解释状态。动画只表达进入、选择、提交、解锁与导航，不制造持续噪声。
5. 手机不是桌面的缩小版。手机端重新安排信息顺序，并使用固定底部任务导航。
6. 隐私契约高于视觉。任何动效或样式都不能暴露后端没有返回的身份字段。

## 3. 色彩令牌

| 令牌 | 色值 | 语义 |
| --- | --- | --- |
| `--bg` / `--neo-cream` | `#FFFCF0` | 页面暖纸底色 |
| `--surface` / `--neo-paper` | `#FFFFFF` | 主卡片与表单 |
| `--text` / `--neo-ink` | `#17150F` | 文字、边界、硬阴影 |
| `--dim` / `--neo-muted` | `#6B675A` | 说明和元信息 |
| `--surface2` / `--neo-yellow` | `#FFE95C` | 选中、高亮与 chip；不做大面积背景 |
| `--primary` / `--neo-purple` | `#5B4DFF` | 主动作、焦点和关键数字 |
| `--accent` / `--neo-green` | `#58CC02` | 已连接、成功和进度；不配白字 |
| `--hot` / `--neo-coral` | `#FF5A36` | 优惠、未读和警告 |
| literal white | `#ffffff` | 信息面板与对比底色 |

严禁使用渐变。色彩必须是清晰、可复现的纯色块。

## 4. 线条、阴影与圆角

- 统一边框：`2.5px solid var(--text)`。
- 主投影：`6px 7px 0 var(--text)`；卡片投影：`4px 5px 0 var(--text)`。
- hover 投影：`3px 4px 0 var(--text)`；active 投影：`0 0 0 var(--text)`。
- 面板圆角：16px；输入与消息圆角：11px；按钮和状态使用胶囊圆角。
- 所有阴影禁止模糊，避免变成柔和 SaaS 卡片。
- 按下态通过位移抵消部分硬阴影，形成真实的机械反馈。

## 5. 字体层级

- 字体使用系统中文字体栈，不请求外部资源。
- 一级标题使用极粗字重，紧凑行高和轻微负字距。
- 英文 eyebrow 使用大写、宽字距、小字号，表达实验板编号。
- 正文保持舒适行高，不用全大写，不用浅灰低对比文字。
- 数据值使用等宽数字特性，匹配分与进度值必须一眼可辨。

推荐层级：

| 层级 | 规格 | 说明 |
| --- | --- | --- |
| H1 | `23px / 600` | 页面主标题 |
| H2 | `18px / 600` | 区域标题 |
| H3 | `16px / 600` | 卡片标题 |
| Body | `13.5px / 500` | 正文与表单 |
| Primary action | `14.5px / 600` | 主按钮 |
| Chip | `10.5px / 600` | 标签、编号、状态 |

## 6. 间距系统

基础单位为 4px，常用间距为 8、12、16、20、24、32、40、56、72px。

- 页面左右安全边距：桌面 32px，平板 22px，手机 14–18px。
- 卡片内边距：桌面 24–36px，手机 18–22px。
- 控件最小高度：44px；手机主要按钮建议 52px。
- 底部导航必须包含 `env(safe-area-inset-bottom)`。
- 固定底部导航出现时，页面底部必须留出完整导航高度加 24px。

## 7. 组件语义

### 7.1 按钮

- 主按钮：紫底黑边，白字，硬阴影。
- 次按钮：黄底黑边，黑字，硬阴影。
- 成功动作：绿底黑边。
- 危险动作：珊瑚底黑边；危险次动作使用白底珊瑚强调但保留黑边。
- hover 仅做 1–2px 位移和色块切换；active 再下沉；focus-visible 使用 3px 外描边。

### 7.2 卡片

- 卡片首先是信息边界，不能只靠背景色区分。
- 标题区可用黄色带；主结果区可用紫色或绿色。
- 相邻卡片通过编号、色彩语义或内容结构产生节奏，不随机换色。
- 桌面可使用错位阴影；手机端降低偏移，避免造成横向溢出。

### 7.3 表单

- 输入框、选择框、文本域均使用 2.5px 黑边、暖白底和明确 label。
- 选择卡片在 `:has(input:checked)` 时切换为黄色或绿色，并保留文本状态。
- 服务器提交时只显示视觉提交状态，不更改 action、method、name 或数据规则。
- 错误由服务端 flash 返回，前端不可复制业务校验。

### 7.4 数据与进度

- 匹配分是视觉锚点，但不能展示原始分或权重。
- 进度条使用黑色轨道、绿色或紫色填充，必须有文字标签。
- 问号卡代表后端隐藏的共同点；它们是隐私边界，不是加载占位符。

### 7.5 导航

- 桌面使用顶部普通链接与 Flask `url_for()`。
- 登录后的手机端使用固定底部任务导航，仍然是普通 `<a href>`。
- 当前页由服务端输出 `.is-active` 和 `aria-current="page"`，浏览器不重算路由状态。
- 不得加入前端路由、Hash 路由或单一 HTML 容器。

## 8. 动效规范

| 时长 | 用途 |
| --- | --- |
| `160ms` | 按压、焦点、颜色反馈 |
| `280ms` | 卡片进入、选择状态、导航状态 |
| `520ms` | 页面首屏主区进入、分数计数 |

- 页面进入使用小幅 Y 位移、透明度和错峰，不使用大幅飞入。
- 不同组件采用不同但一致的动效角色：卡片抬升、标签弹入、数据计数、进度展开、消息轻推入。
- 链接离开页只做约 140ms 的轻微压暗，不阻断浏览器原生导航。
- 表单提交进入 `.is-submitting` 状态，按钮保持可读且避免重复点击。
- `prefers-reduced-motion: reduce` 下必须关闭位移、计数和非必要过渡。

## 9. 手机端规则

- 重点验收宽度：320、360、390、430px。
- 不依赖 hover 才能发现动作。
- 聊天消息、活动元信息和表单字段必须单列重排。
- 固定底部导航不能遮挡 composer、危险操作或页脚。
- 长中文标题允许自然换行；英文代码和核销码使用 `overflow-wrap:anywhere`。
- 输入框使用 PRD 的 13.5px 字号与至少 44px 高度；浏览器缩放风险需在后续真实 iOS QA 中单独验证。
- 横屏时底部导航压缩为单行任务带，内容仍可滚动。

## 10. 可访问性与降级

- 所有动作保留原生链接、按钮、表单与键盘顺序。
- 颜色不能成为唯一状态信号；状态同时有文字、边界或 `aria-current`。
- `:focus-visible` 的轮廓必须在所有纯色底上清晰可见。
- 动画脚本失效时页面仍完全可读、可导航、可提交。
- 强制颜色模式保留边框、当前页和按钮语义。
- skip link、flash live region、聊天 `role="log"` 与表单 label 不得删除。

## 11. 资产清单

| 资产 | 位置 | 用途 |
| --- | --- | --- |
| 视觉参考 | `docs/design-reference-brutalist.png` | 设计方向与 QA 比对 |
| 纸张点阵纹理 | `app/static/img/paper-dot-grid.png` | 页面背景，低对比铺陈 |
| 品牌标记 / favicon | `app/static/img/brand-mark.png` | 页头识别与浏览器标签 |
| 摇骰子工具图 | `app/static/img/chat-tool-dice.webp` | 会话“摇骰子出话题”任务卡 |
| 任务卡工具图 | `app/static/img/chat-tool-task.webp` | 会话“抽任务卡”任务卡 |
| 解锁工具图 | `app/static/img/chat-tool-unlock.webp` | 会话“匹配点解锁”任务卡 |

纸张纹理由内置图像生成工具按「暖象牙色、极浅规则点阵、无文字、无图标、无渐变」生成。它必须保持低对比，不可抢夺正文注意力。

## 12. 代码分层

- `app.css`：旧版结构与基础回退，继续保留。
- `brutalist-foundation.css`：兼容令牌、全局外壳、控件与动效基础。
- `prd-contract.css`：最后加载的 PRD 第 5 章运行时收口层。
- `brutalist-components.css`：全部功能页组件皮肤与页面结构。
- `brutalist-mobile.css`：响应式重排、底部任务导航与触控适配。
- `motion.js`：无依赖渐进增强动效，不承担业务状态或路由逻辑。

后续新增样式应放入对应层，不要继续扩张单一巨型文件。

## 13. 不可破坏的契约

- 保留 Flask + Jinja2 服务端渲染。
- 保留每个页面的独立路由和模板。
- 保留所有 `<form>` 的 method、action、字段 name 与最大长度。
- 不将候选人身份、原始分、权重或完整用户对象写入 DOM。
- 不在客户端生成聊天工具结果、关系解锁、活动状态或匹配结果。
- 不使用 React、Vue、前端路由、Hash 路由或单一 `index.html`。

## 14. 匿名匹配流程扩展（2026-08-23）

本轮是 **Extension**，只参考 `prototype2.0.html` 的产品状态流，不还原其视觉。既有新粗野主义品牌、页面结构、顶部导航和移动端任务导航全部保留。

Design Read：手机优先的 Flask SSR 匿名匹配旅程；目标用户是已完成资料的登录用户；视觉语言继续使用“可信关系实验板”；模式为 Extension；视觉变化度 `3/10`、动效强度 `7/10`、信息密度 `4/10`、资产依赖 `2/10`、品牌忠实度 `10/10`。

```text
首页双入口
  → GET /matches（待匹配说明）
  → POST /matches/search/start
  → GET /matches/searching（三阶段过程动画）
  → POST /matches/search/complete
  → GET /matches/<candidate_id>（单个匿名结果）
      ├─ POST /matches/search/retry → 新一轮匹配
      └─ POST /matches/<candidate_id>/start → 匿名会话
```

- 三阶段节奏沿用参考流程的 `260ms / 1000ms / 1740ms / 2500ms`，分别对应硬性筛选、相似度计算、匿名排序和完成。
- `match-flow.js` 只控制展示时序与提交普通 POST 表单；候选选择、尝试令牌、取消、旧页面失效和结果确认全部由 Flask session 状态机处理。
- 匹配中 HTML 不包含候选 ID、匿名代号、分数、标签或结果 JSON。
- 无 JavaScript 时可直接提交“查看结果”；减弱动画模式立即完成步骤并进入服务端结果。
- 匹配中允许取消；成功后“换一位”会创建新的 `attempt_id`，旧页面不能覆盖新流程。
