[project]
name = "real-tags-hackathon"
version = "0.1.0"
description = "Anonymous matching and interest dinner hackathon demo"
requires-python = ">=3.12,<3.14"
dependencies = [
  "Flask>=3.1,<4",
]

[tool.uv]
package = false
