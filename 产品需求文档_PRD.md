# 真实标签 · 匿名匹配社交产品 PRD

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 真实标签 × 匿名匹配 × 游戏化破冰 × 线下兴趣饭局 社交产品需求文档 |
| 版本 | V1.0（黑客松 MVP） |
| 更新日期 | 2026-08-22 |
| 阅读对象 | 产品、研发、设计 |
| 文档状态 | 待评审 |

---

## 1. 一句话说明产品

一个「用你在别的 App 里的真实行为数据来匹配人、匹配上之后先匿名聊天、靠小游戏破冰、还能约一桌同频的人线下吃饭」的兴趣社交产品。

用户不需要费劲填资料，我们从多邻国、Keep 这类第三方 App 拉取他的真实行为数据，自动生成标签；匹配时双方**只看到一个匹配度百分比**，看不到照片、年龄、学历；想知道对方是谁，就得聊下去，聊得越久解锁越多。线上聊得来之后，产品提供 **3–10 人的线下兴趣饭局**作为轻社交出口，餐厅也能自己发起饭局并让利获客。

产品由四个功能点组成：**① 真实数据接入 ② 匿名匹配机制 ③ 游戏化破冰工具 ④ 线下约饭局**。前三个解决线上「信不信、怎么选、聊什么」，第四个解决线上关系怎么落到线下。

---

## 2. 用户需求与痛点

### 2.1 目标用户

**C 端用户**

| 用户类型 | 典型诉求 | 举例 |
| --- | --- | --- |
| 找垂直搭子的人 | 想找一个能一起做某件具体事的人 | 「我想找个学英语的搭子每天陪我说 10 分钟」 |
| 想认真聊天的人 | 厌倦看脸、厌倦刷卡片，想找聊得来的 | 「我要精神层面的匹配，不是看颜值」 |
| 社恐/不会破冰的人 | 匹配上了但不知道说什么，一句「你好」之后就死了 | 「加上好友第二天就沉了」 |
| 想线下认识同频人的人 | 想扩圈但抗拒一对一见面的压力 | 「我想认识做 AI 的人，但不想一对一相亲式吃饭」 |
| 新到一个城市的人 | 没有本地社交圈，需要低门槛入口 | 「刚来上海，周末不知道跟谁吃饭」 |

**B 端用户（餐厅商家）**

| 用户类型 | 典型诉求 | 举例 |
| --- | --- | --- |
| 中小餐厅 / 咖啡馆 | 空闲时段没人，获客成本高 | 「周二到周四晚上空一半桌，愿意打折换客流」 |
| 有主题氛围的店 | 想吸引特定人群、做内容 | 「我想把店做成 AI 从业者常来的地方」 |

### 2.2 四个核心痛点

**痛点 1：资料全靠自填，真实性没法验证。**
现在所有社交 App 的个人资料都是用户自己写的，写「热爱运动」「正在学日语」没有任何成本，也没有任何证据。用户对匹配到的人的信息真实性有强烈的验证需求，但产品没给他任何抓手。

**痛点 2：刷卡片放大了颜值和学历，反而让人更难做决定。**
市面产品的主流形态是刷卡片，卡片上放大的是照片、身高、学校、职业。但根据**选择悖论（Paradox of Choice）**：可选项整体质量的提升并不会提高决策者的满意度，反而会不断抬高他的选择标准，最终导致更难决策、更容易后悔、更容易空虚。看得越多，越挑，越不满意。

**痛点 3：陌生人破冰极难，匹配即终点。**
匹配成功不等于关系开始。陌生人之间没有共同话题、没有共同经历，聊天窗口打开后双方都在等对方先说，绝大多数会话止于前三句。

**痛点 4：社交方式被 1V1 dating 垄断，线下没有低压力的入口。**
国内主流社交产品几乎都是 1V1 dating 的变体，线下环节默认是「两个人见面吃饭」。这套形式对大量用户并不适用：一对一见面压力极大、双方全程互相审视、聊崩了没有退路，导致很多人长期停留在线上、从不见面。而真实需求是——我想认识一群同频的人，不是找对象。缺的不是匹配，是一个**以共同兴趣为钩子的多人轻社交场景**。

同时对餐厅侧也存在未被满足的需求：中小餐厅空闲时段大量闲置，线上获客成本极高，愿意让出一部分利润换真实客流，但缺少能精准把「一群人」凑到店里的工具。

### 2.3 四个功能点分别解决了什么

| 功能点 | 对应痛点 | 解决方式 | 用户感知的价值 |
| --- | --- | --- | --- |
| ① 真实数据接入 | 痛点 1：数据不可信 | 标签来自第三方 App 的真实行为数据（多邻国学习天数、Keep 运动记录），不是用户自填 | 「他标的『正在学日语』是有 300 天打卡记录撑着的」 |
| ① 真实数据接入 | 匹配维度太浅 | 行为数据自动扩充标签维度，算法能拿到远超用户自填的信息量（学什么语言、学多久、几点学、运动类型、运动频率） | 「我想找学英语的搭子，系统真的能懂我要什么」 |
| ② 匿名匹配机制 | 痛点 2：选择悖论 | 匹配结果只给百分比，不给照片/年龄/标签明细。人的属性从「一堆可比较的参数」收敛成「一个数字 + 一个人」 | 决策成本骤降，不再横向比价，注意力回到聊天本身 |
| ② 渐进式信息解锁 | 关系推进无路径 | 按聊天天数/互动热度分阶段解锁对方信息，最终可交换联系方式 | 关系有进度条，聊下去有明确回报 |
| ③ 游戏化破冰工具 | 痛点 3：破冰难 | 聊天框内置破冰工具箱（摇骰子出话题、抽任务卡、大富翁、扑克牌），把「我该说什么」变成「按提示做就行」 | 不用自己想话题，尴尬由工具兜底 |
| ③ 匹配点解锁任务 | 痛点 3 + 留存 | 双方的匹配点位初始全部显示为「？」，靠聊天逐个解锁，全解锁给奖励 | 神秘感 + 探索感，有理由继续聊 |
| ④ 线下约饭局（C 端发起） | 痛点 4：1V1 压力大 | 用 3–10 人的多人局替代 1V1 见面，以共同兴趣（对 AI 感兴趣、创业、学英语）为钩子而非颜值条件 | 「不是相亲，是认识一桌同频的人」，压力被人数分散 |
| ④ 线下约饭局（B 端发起） | 痛点 4 + 商业闭环 | 餐厅发起主题饭局并让利（折扣券/满减/免费小食），用空闲时段换真实客流 | 用户拿到折扣，餐厅拿到客流，平台拿到线下场景与变现路径 |
| ④ 线下约饭局 | 线上关系无出口 | 匹配、聊天积累的关系有了落地场景；饭局同桌的人还能回到 1V1 匿名会话继续聊 | 关系不止步于聊天框 |

---

## 3. 核心用户链路

```
注册/登录
   ↓
授权接入第三方 App（多邻国 / Keep / …，可跳过）
   ↓
系统生成真实标签（仅对用户本人可见）
   ↓
系统按后端配置的权重比率计算匹配度（权重对用户完全不可见，无需用户配置）
   ↓
进入匹配 → 只看到一个匹配度百分比（如 87%）
   ↓
建立匿名会话：头像为默认占位、无昵称真名、无年龄照片
   ↓
聊天 + 破冰工具箱（摇骰子/任务卡/小游戏）+ 匹配点解锁任务（初始全为"？"）
   ↓
随聊天天数/热度提升，阶段性解锁对方信息 → 最终可交换联系方式
```

**线下约饭链路（与上面的线上链路并行，共用同一套标签体系）：**

```
【C 端发起】                              【B 端餐厅发起】
用户填写：时间/餐厅/人数(3-10)/           商家填写：门店/主题/空闲时段/
想约的人的特征标签                         桌型人数/目标人群标签/让利形式
        ↓                                        ↓
                    活动进入「活动广场」
                （按标签匹配度排序，展示"这个局和你的匹配度 82%"）
                              ↓
              用户报名（先到先得 / 发起人审核，审核只看匹配度不看脸）
                              ↓
                   截止时达到成团下限 → 自动成团
                              ↓
        创建 3-10 人临时群聊 + 发放商家券（B 端局）+ 多人破冰工具
                              ↓
                     线下到店 → 扫码核销券
                              ↓
        活动结束 → 群聊 7 天后归档；同桌成员可单独发起 1V1 匿名会话
```

---

## 4. 功能需求细化

### 4.1 模块一：真实数据接入与标签体系

#### 4.1.1 数据源

| 数据源 | 可拉取内容 | MVP 是否接入 | 备注 |
| --- | --- | --- | --- |
| 多邻国 Duolingo | 学习语种、连续打卡天数、总学习天数、活跃时段、等级 | ✅ 真实接口优先，接不通则 mock | 会上确认：真实用户可有 971 天 / 3 年量级数据，是很强的可信度证明 |
| Keep | 运动类型、周运动次数、累计里程/时长、活跃时段 | ✅ 真实接口优先，接不通则 mock | 会上确认可调 |
| 微信 / 抖音 | 兴趣偏好、内容消费标签 | ❌ 不做，仅预留 | 属本地数据采集，PC 端数据不全、需额外接口，成本高；且团队 4 人无法逐个真实采集 |
| 用户自填 | 注册时填写的 8 个基础标签（见 4.1.2） | ✅ | 兜底维度，用于第三方数据缺失时也能完成匹配 |

**研发实现约定（会上明确结论）：**
1. **接口层与数据层必须解耦。** 定义统一的 `DataSourceAdapter` 抽象，真实 API 和 mock 数据实现同一套接口。
2. **MVP 阶段以 mock 数据为主**，但对外呈现的字段结构、鉴权流程、错误码必须按真实接入设计，Demo 时可讲清"真实接口已预留"。
3. mock 数据需覆盖至少 20+ 标签，且每个标签带「数据来源」字段，前端要展示来源徽标（这是真实性的可视化证明）。

#### 4.1.2 标签清单

标签分两类：**用户自填标签**（注册时填，用于兜底和基础筛选）和 **API 行为标签**（第三方拉取，用于真实性与深度匹配）。

**A. 用户自填标签（注册流程，8 个）**

注册时一次性填完，控制在 8 个以内，避免注册流失。除性别/年龄外均可跳过。

| # | 标签 | 类型 | 取值示例 | 是否必填 | 用途 |
| --- | --- | --- | --- | --- | --- |
| 1 | 年龄 | 数值 | 出生年份，展示为年龄段 | 必填 | 基础筛选（硬性过滤） |
| 2 | 性别 & 希望匹配性别 | 枚举 | 男 / 女 / 不愿透露；希望匹配：男 / 女 / 不限 | 必填 | 基础筛选（硬性过滤） |
| 3 | 城市 | 枚举 | 北京 / 上海 / 海外… | 必填 | 权重维度 + 线下场景预留 |
| 4 | 交友目的 | 多选枚举 | 学习搭子 / 运动搭子 / 聊天倾诉 / 兴趣同好 / 饭搭子 / 随便聊聊 | 必填 | **最重要的自填维度**，默认权重最高 |
| 5 | 兴趣爱好 | 多选枚举 | 音乐 / 影视 / 游戏 / 健身 / 旅行 / 摄影 / 美食 / 二次元 / 阅读 / 宠物 | 选填 | 权重维度，共同兴趣数量参与打分 |
| 6 | MBTI 性格 | 枚举 | INFP / ENTJ… / 不知道 | 选填 | 权重维度，年轻用户接受度高 |
| 7 | 星座 | 枚举 | 白羊…双鱼 | 选填 | 权重维度，轻社交话题好用 |
| 8 | 作息类型 | 枚举 | 早鸟 / 正常 / 夜猫子 | 选填 | 权重维度，直接影响能不能聊得上（时间重叠） |

> 说明：年龄、性别属于**硬性过滤条件**，不参与 100% 权重分配，先过滤候选池再算匹配度。

**B. API 行为标签（第三方拉取，`verified: true`）**

| 数据源 | 标签 | 类型 | 取值示例 | MVP |
| --- | --- | --- | --- | --- |
| 多邻国 | 在学语种 | 多选枚举 | 日语 / 英语 / 韩语 / 西语 | ✅ |
| 多邻国 | 连续打卡天数（streak） | 数值 | 971 天 | ✅ |
| 多邻国 | 学习坚持度 | 分级 | 由 streak + 周活跃天数换算：轻度 / 稳定 / 硬核 | ✅ |
| 多邻国 | 学习活跃时段 | 时段 | 主要在 22:00–24:00 学习 | ✅ |
| 多邻国 | 当前等级 / 总 XP | 数值 | Lv.12 / 48000 XP | ✅ |
| 多邻国 | 联赛段位 | 枚举 | 黑曜石 / 钻石联赛 | P1 |
| Keep | 主要运动类型 | 多选枚举 | 跑步 / 骑行 / 瑜伽 / 力量训练 / 健走 | ✅ |
| Keep | 周运动频次 | 数值 | 4 次/周 | ✅ |
| Keep | 累计运动量 | 数值 | 累计 320 km / 累计 86 小时 | ✅ |
| Keep | 运动活跃时段 | 时段 | 晨练型 / 夜跑型 | ✅ |
| Keep | 运动强度等级 | 分级 | 由配速/时长换算：入门 / 进阶 / 资深 | ✅ |
| Keep | 连续打卡周数 | 数值 | 连续 12 周 | P1 |
| 预留扩展 | 音乐口味（Spotify / 网易云）、游戏偏好（Steam）、技术栈（GitHub）、读书口味（微信读书）、内容兴趣（抖音） | — | — | ❌ V2 |

跨源可推导的复合标签（研发在标签生成层计算，用于提升匹配质量）：

| 复合标签 | 推导方式 | 匹配价值 |
| --- | --- | --- |
| 自律程度 | 多邻国 streak + Keep 连续打卡周数 综合分级 | 高自律用户之间更容易成为长期搭子 |
| 活跃时间重叠度 | 学习活跃时段 ∩ 运动活跃时段 → 用户日常在线时间画像 | 直接决定两人能不能聊起来 |
| 目的一致性 | 自填「交友目的」与行为标签是否自洽（填了学习搭子且真有学习数据） | 真实性校验，可用于可信度徽标 |

#### 4.1.3 标签结构（给研发的数据模型参考）

```json
{
  "userId": "u_10086",
  "tags": [
    {
      "tagId": "lang_learning_ja",
      "category": "学习",
      "name": "正在学日语",
      "value": { "language": "ja", "streakDays": 971, "level": 12 },
      "source": "duolingo",
      "verified": true,
      "visibility": "self_only",
      "updatedAt": "2026-08-22T10:00:00Z"
    },
    {
      "tagId": "sport_running",
      "category": "运动",
      "name": "跑步爱好者",
      "value": { "weeklyTimes": 4, "totalKm": 320 },
      "source": "keep",
      "verified": true,
      "visibility": "self_only"
    }
  ]
}
```

关键字段说明：
- `verified`：是否来自第三方真实数据。自填标签为 `false`。
- `visibility`：**MVP 阶段全部为 `self_only`**，即标签只对用户本人展示，不对匹配对象展示（见 4.1.4 的会议决议）。
- `source`：数据来源，前端展示"来自多邻国"的来源标识。

#### 4.1.4 标签展示规则（重要，会上曾有分歧，已达成一致）

| 场景 | 是否展示标签 | 说明 |
| --- | --- | --- |
| 用户看自己 | ✅ 全部展示 | 用户需要看到自己有哪些标签，才能配置匹配倾向 |
| 用户看匹配对象（匹配阶段） | ❌ 不展示 | 只展示匹配度百分比 |
| 用户看匹配对象（聊天阶段） | ⏳ 按解锁进度逐步展示 | 见 4.2.3 |
| 任何用户看匹配权重规则 | ❌ 永不展示 | 权重是后端算法参数，双方都看不到，产品内也不解释比例 |
| 线下约饭场景 | ⏳ 另有规则 | 报名/审核只见匹配度与共同标签数，成团群聊内见匿名代号 + 1–2 个兴趣标签，详见 4.5.8 |

> 会议原话要点：「我导进来的数据只对我展示，不会对别人展示」「我们的匹配机制不对其他人做展示，只对用户自己展示」。

---

### 4.2 模块二：匿名匹配机制

#### 4.2.1 匹配权重规则（后端配置，用户不可见、不可调）

**核心规则：权重是底层算法参数，由开发团队在服务端配置，把 100% 按比例分配到各个标签上。用户端没有任何权重配置入口，用户完全无感。**

明确边界，避免研发理解偏差：
- **不做**用户端的权重配置页、滑块、拖拽格子等任何交互。
- 用户在产品里能做的只有两件事：注册时填 8 个基础标签、授权第三方 App。剩下的全由算法决定。
- 权重的取值由开发团队定，后续通过 A/B 测试对比不同权重组合下的数据表现（匹配后开聊率、聊天天数、解锁阶段推进率）来选优。**本次 Demo 不做 A/B 测试，直接用下面这套默认权重。**

**Demo 默认权重表（V1，合计 100%）：**

| 标签 | 权重 | 来源 | 为什么给这个权重 |
| --- | --- | --- | --- |
| 交友目的 | 30% | 自填 | 目的不一致，其他维度再像也聊不下去，必须占最大头 |
| API 行为标签（在学语种 / 主要运动类型，取用户已授权的） | 25% | 多邻国 / Keep | 产品的差异化卖点，真实数据必须有足够话语权 |
| 兴趣爱好 | 20% | 自填 | 共同话题的直接来源 |
| 活跃时间重叠度（作息类型 + 活跃时段） | 15% | 自填 + API 复合 | 时间对不上就聊不起来，是隐性但强相关的因子 |
| 城市 | 5% | 自填 | 匿名线上聊天为主，同城不是刚需，仅作弱加分 |
| MBTI | 5% | 自填 | 弱信号，只做微调 |
| **合计** | **100%** | | |

**兜底规则：** 若用户未授权任何第三方 App，行为标签的 25% 按 30:20 的比例回补到「交友目的」和「兴趣爱好」，即变为 **交友目的 45% + 兴趣爱好 30% + 活跃时间重叠度 15% + 城市 5% + MBTI 5% = 100%**。

**按交友目的差异化的权重组（P1，时间允许再做，同样由团队配置、用户不可见）：**

用户注册时填的「交友目的」可以直接作为选权重组的开关，让匹配更贴合场景。每组合计 100%。

| 交友目的 | 权重组 |
| --- | --- |
| 学习搭子 | 在学语种 35% + 学习坚持度 20% + 学习活跃时段 15% + 兴趣爱好 20% + 城市 10% |
| 运动搭子 | 主要运动类型 30% + 周运动频次 20% + 运动活跃时段 15% + 城市 25% + 兴趣爱好 10% |
| 聊天倾诉 | 兴趣爱好 35% + MBTI 25% + 作息类型 20% + 活跃时间重叠度 15% + 星座 5% |
| 兴趣同好 | 兴趣爱好 40% + API 行为标签 25% + 活跃时间重叠度 20% + 城市 15% |

**研发实现要求：**
1. 权重必须写在 **统一的配置文件 / 常量表** 里（如 `matchWeights.config.ts`），禁止散落在业务逻辑中硬编码，方便一处改动全局生效、快速调参。
2. 配置结构预留 `groupKey` 字段（对应交友目的），即使 Demo 只启用一组默认权重，后续加权重组不需要改算法代码。
3. 每次调参后各组权重之和必须校验等于 1，建议加一个启动时的断言。

**匹配度计算：**

```
第一步 硬性过滤：按年龄区间、希望匹配性别过滤候选池（不占权重）

第二步 逐标签打分：对候选池中每个用户，计算每个参与标签的相似度 sim_i ∈ [0, 1]
  - 枚举型（学习语种、运动类型、城市、星座、作息）
      完全相同 = 1.0；同大类不同项 = 0.5；无交集 = 0
  - 多选枚举型（兴趣爱好、交友目的、在学语种）
      Jaccard 相似度 = 交集数量 / 并集数量
  - 数值型（打卡天数、周运动频次、累计运动量）
      1 - |A - B| / 该标签归一化区间上限，结果下限截断为 0
  - 时段型（学习/运动活跃时段、活跃时间重叠度）
      重叠小时数 / 并集小时数
  - 分级型（学习坚持度、运动强度、自律程度）
      1 - |等级差| / (总等级数 - 1)
  - MBTI
      4 个字母逐位比较，相同位数 / 4

第三步 加权求和：
  匹配度 = Σ (w_i × sim_i) × 100%    其中 Σ w_i = 1（即 100%）

第四步 展示平滑：把原始分映射到 60%–98% 区间后展示
```

展示层必须做平滑，避免出现 12% 这类挫伤体验的低分（建议映射区间 60%–98%），具体映射函数与阈值由研发和产品在联调阶段共同校准。**注意：平滑只作用于展示，排序仍按原始分。**

**权重的可见性：** 权重规则是后端算法参数，**不对任何用户展示**，产品内也不解释具体比例。用户只看到最终那个匹配度数字（见 4.1.4）。

#### 4.2.2 匹配结果页

- **只展示一个数字**：匹配度百分比 + 一句氛围文案（如「你们有 4 个隐藏共同点」）。
- 不展示：照片、真实昵称、年龄、性别、学历、职业、标签明细。
- 头像统一为系统占位图形，昵称为系统生成的匿名代号。
- 匹配点位以 **「？」占位卡** 呈现，例如 `? ? ? ?`，明确告知用户「有 4 个匹配点，聊天解锁」。

> 设计原理（可用于路演讲述）：告诉用户"匹配度多少"，但不告诉他"匹配在哪里"，制造新鲜感与探索感，同时从机制上规避选择悖论。

#### 4.2.3 渐进式信息解锁

解锁维度以**聊天天数**为主，辅以互动热度值（消息条数、游戏参与次数、语音时长）。

| 阶段 | 达成条件（建议值，可调） | 解锁内容 |
| --- | --- | --- |
| L0 完全匿名 | 匹配成功 | 仅匹配度 + 匿名代号 + 「？」匹配点 |
| L1 初识 | 聊天满 1 天且双向发言 ≥ 10 条 | 解锁 1 个匹配点、城市、年龄段 |
| L2 熟悉 | 聊天满 3 天 | 解锁 2 个匹配点、模糊头像、兴趣类别 |
| L3 熟络 | 聊天满 7 天 | 解锁全部匹配点、清晰头像、完整标签 |
| L4 走出产品 | 全部匹配点解锁 | 开放交换联系方式（微信等）+ 解锁奖励 |

规则要点：
- **双向条件**：单方刷消息不算，必须双方都有发言才计入有效聊天日。
- **不可逆**：已解锁不回退。
- **奖励机制**：全部匹配点解锁后给到奖励（可为称号、皮肤、额外匹配次数），具体形式待定。
- 参考产品：Soul、陌陌、探探的热度值/关系升级机制。

---

### 4.3 模块三：破冰游戏工具（聊天内工具箱）

#### 4.3.1 形态定位（会上明确决策）

**结论：以「聊天」为核心场景，游戏作为聊天框内的工具箱入口，不做独立游戏房间。**

被否决的方案：「多人语音游戏房间」（类似鹅鸭杀 + 匹配机制，把多个匹配到的人拉进同一房间一起玩）。

否决原因（会议记录整理）：
1. 陌生人在初次匹配时普遍不愿意开语音，语音类游戏起不来。
2. 无语音的轻游戏用户投入时间短、兴趣衰减快，很快就会想换房间或重新匹配。
3. 换房成本低导致关系无法沉淀，与产品目标（深度聊天）冲突。
4. 网易已有同类产品验证过，且开发难度高，黑客松周期内不可控。

#### 4.3.2 工具箱功能清单

工具箱入口位于聊天输入框旁（类似"+"号面板），点击展开。

| 工具 | 玩法 | 输出 | 优先级 |
| --- | --- | --- | --- |
| 摇骰子（摇卦） | 点击摇出 1–6 点 | 每个点数对应一个聊天话题，直接推送到聊天框 | P0 必做 |
| 抽任务卡 | 抽一张任务/问题卡 | 卡片内容作为双方的聊天任务或话题，如「说出你最近最想学的东西」 | P0 必做 |
| 匹配点解锁任务 | 展示「？」占位，完成话题任务后解锁一个匹配点 | 解锁动画 + 揭示一个共同点 | P0 必做 |
| 扑克牌 | 轻量抽牌对话游戏 | 牌面对应互动指令 | P1 |
| 大富翁 | 双人轻量棋盘，格子触发话题/任务 | 走格子推进话题 | P2（时间允许再做） |

#### 4.3.3 交互要求

- 工具触发的结果以 **系统卡片消息** 形式插入聊天流，双方都能看到，避免单向。
- 工具需同时支持 **1V1 会话** 与 **约饭活动群聊（3–10 人）** 两种场景，研发在设计时按"会话"抽象而非按"两人"抽象，避免后面为群聊重写一遍（见 4.5.7）。
- 任务卡内容库 MVP 至少准备 30 条，按「学习 / 运动 / 生活 / 脑洞 / 价值观」分类，与用户匹配标签弱关联（例如双方都有学习标签时优先出学习类话题）。
- 每次工具使用计入互动热度值，与 4.2.3 的解锁进度打通。

---

### 4.4 模块四：聊天基础能力

| 能力 | 要求 | 优先级 |
| --- | --- | --- |
| 文字聊天 | 标准聊天框，支持文字、表情 | P0 |
| 语音聊天 | 产品**鼓励**语音，入口显著；但不强制，用户可全程文字 | P1 |
| 系统卡片消息 | 承载工具箱结果、解锁提示、阶段升级提示 | P0 |
| 关系进度条 | 聊天页顶部展示当前解锁阶段与下一阶段条件 | P0 |
| 匿名保护 | 聊天页不暴露任何未解锁信息，禁止截图外显真实资料 | P0 |
| 多人群聊 | 支持 3–10 人的约饭活动群聊，成员以匿名代号显示 | P0 |

> 会话模型要求：1V1 会话与约饭群聊**共用同一套会话/消息模型**，只用 `type` 区分（`direct` / `event_group`），避免实现两套聊天。

---

### 4.5 模块五：线下约饭局（功能点四）

#### 4.5.1 场景定位

**一句话：以「相同兴趣」为钩子的 3–10 人线下饭局，不是相亲，不是 1V1 约会。**

国内主流社交产品的线下终点几乎都是「两个人见面吃饭」，这种形式压力极大：一对一坐下来，双方都在被审视，聊崩了没有退路，所以很多人宁可一直线上聊也不见面。3–10 人的兴趣局把压力分散掉了——大家是因为「都对 AI 感兴趣」「都在创业」才坐到一桌，话题天然存在，某个人聊不来也不影响整场，走出来的社交关系反而更自然。

| 维度 | 1V1 dating（市面主流） | 本功能：兴趣饭局 |
| --- | --- | --- |
| 人数 | 2 人 | 3–10 人 |
| 钩子 | 颜值、条件 | 共同兴趣 / 共同身份（对 AI 感兴趣、创业者、学英语） |
| 心理压力 | 高，全程被审视 | 低，压力被人数分散 |
| 目标 | 建立亲密关系 | 认识一群同频的人，轻社交 |
| 失败成本 | 高，聊崩就结束 | 低，一桌里总有能聊的 |

#### 4.5.2 两类发起方

| 发起方 | 动机 | 能做什么 |
| --- | --- | --- |
| **C 端用户** | 想找一桌同频的人吃饭聊天 | 自己定时间、餐厅、人数、想约什么样的人 |
| **B 端餐厅** | 引流获客，填充空闲时段 | 发起主题饭局并**让利**（折扣券、满减、免费小食等）吸引用户成团到店 |

B 端是这个功能的商业闭环：餐厅出让一部分利润换真实客流，平台提供成团能力和精准人群，用户拿到折扣。三方都有收益，也是这个功能区别于纯社交产品的地方。

#### 4.5.3 C 端发起约饭

**发起表单字段：**

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| 活动主题 | 文本 | 必填 | 如「聊聊 AI 创业，周五晚」 |
| 餐厅 / 地点 | POI 选择 | 必填 | **必须绑定真实餐厅 POI**，不允许自定义私人地址（安全要求） |
| 时间 | 日期 + 时段 | 必填 | 精确到 30 分钟 |
| 人数上限 | 数值 | 必填 | 限制 3–10 人，含发起人 |
| 成团下限 | 数值 | 必填 | 默认 3 人，不足自动取消 |
| 人均预算 | 枚举 | 必填 | 50 以下 / 50–100 / 100–200 / 200+ |
| 费用方式 | 枚举 | 必填 | AA / 发起人请 / 各付各的 |
| 想约的人的特征 | 标签多选 | 必填，1–5 个 | **直接复用 4.1 的标签体系**，如「对 AI 感兴趣」「创业中」「在学英语」「跑步爱好者」 |
| 性别构成偏好 | 枚举 | 选填 | 不限 / 尽量均衡 / 仅同性别 |
| 报名方式 | 枚举 | 必填 | 先到先得 / 发起人审核 |
| 活动介绍 | 文本 | 选填 | 200 字内 |

**特征标签的作用（这是和 4.1、4.2 复用的地方，研发注意）：**
1. **筛选门槛**：用户与活动要求标签的匹配度低于阈值（建议 40%）时，报名按钮置灰并提示「你和这个局的匹配度较低」，但**不做硬性拦截**，允许用户坚持报名（避免冷启动阶段没人能报）。
2. **推荐排序**：活动广场按「用户标签 × 活动要求标签」的匹配度排序，算法直接复用 4.2.1 的加权打分逻辑，只是把「对比另一个用户」换成「对比一组活动要求标签」。
3. **展示**：活动卡片上展示 **「这个局和你的匹配度 82%」**，与匹配模块的体验保持一致。

#### 4.5.4 B 端餐厅发起约饭

**商家可配置字段：**

| 字段 | 说明 |
| --- | --- |
| 门店 / POI | 关联真实门店 |
| 饭局主题 | 如「AI 从业者交流局」「独立开发者晚餐」 |
| 可用时段 | 商家自己选，通常投放**空闲时段**（如周二至周四晚、下午茶时段） |
| 桌型与容纳人数 | 决定人数上限，3–10 人 |
| 目标人群标签 | 同 C 端，用标签描述想吸引的人群 |
| 让利形式 | 见下表 |
| 场次库存 | 每周可开几场、每场几桌 |

**让利形式（B 端的核心抓手）：**

| 形式 | 说明 | 发放时机 |
| --- | --- | --- |
| 折扣券 | 全单 X 折，仅本次饭局有效 | 成团后发放给全部成员 |
| 满减券 | 满 X 减 Y | 成团后发放 |
| 免费小食 / 饮品 | 到店赠送 | 成团后以权益卡形式发放 |
| 空闲时段特价 | 指定时段人均价下调 | 活动创建时即标注在卡片上 |

**券的规则要求：**
- 券与活动强绑定，**只在本次饭局的时间窗口 + 本门店有效**，防止被当作普通优惠券套利。
- 成团后才发券，未成团不发；活动取消则券自动失效。
- 到店由服务员扫**成团核销码**（一场一码）核销，Demo 阶段做成静态二维码 + 手动点击核销即可。
- 平台侧记录核销数据，作为向商家证明引流效果的依据（这是后续 B 端付费的基础）。

#### 4.5.5 活动广场

- 列表流，卡片展示：主题、时间、餐厅、人均、已报名/上限、**与我的匹配度**、商家券标识（如「到店 8 折」）。
- 筛选维度：时间、商圈/距离、主题标签、人均预算、是否有优惠。
- 排序默认按匹配度，可切换为「时间最近」「离我最近」。
- B 端活动在卡片上有明确的**商家标识**，不伪装成用户发起（透明度要求）。

#### 4.5.6 报名、成团与状态流转

**活动状态机（研发按此实现）：**

```
草稿 → 报名中 → 已成团 → 进行中 → 已结束
                    ↓
                 已取消（未达成团下限 / 发起人取消 / 商家撤场）
```

规则要点：
- 报名截止时间默认为活动开始前 2 小时。
- 截止时达到成团下限 → 自动成团，发送通知 + 发放商家券（若有）。
- 未达下限 → 自动取消并通知全员，已发放的券失效。
- 发起人审核制下，审核时**只能看到报名者的标签匹配度和共同标签数量，看不到照片和个人资料**（延续匿名调性，避免变成看脸筛人）。
- 用户单场活动只能报名一次；同一时间段冲突的活动给出提示。

#### 4.5.7 活动群聊与破冰

- 成团后自动创建**临时群聊**（3–10 人），成员以匿名代号入场。
- **复用 4.3 的破冰工具箱，做多人适配版**：摇骰子出话题（全群可见）、抽任务卡（指定某人回答）、轮流话题接力。多人场景下工具的价值比 1V1 更大，因为群里更容易集体沉默。
- 群内自动推送活动信息卡（时间、地点、导航、券、核销码）。
- 活动结束后群聊保留 7 天后归档；期间成员之间可单独发起 1V1 匿名会话，接入 4.2.3 的渐进解锁机制。

#### 4.5.8 信息可见度规则（重要：解决与匿名机制的冲突）

线下见面必然要露脸，和产品的匿名机制存在天然冲突。这里定一套明确规则，研发不要自行推断：

| 阶段 | 可见信息 | 不可见 |
| --- | --- | --- |
| 浏览活动时 | 活动信息、发起人是用户还是商家、当前报名人数、性别构成比例、与我的匹配度 | 所有成员的身份信息、照片、昵称 |
| 报名后 / 成团前 | 同上 + 成员的**共同标签数量**（如「群里有 3 个人也在学英语」） | 成员个人资料仍不可见 |
| 成团后（群聊内） | 匿名代号 + 每人 1–2 个兴趣标签（用于破冰） | 真实姓名、年龄、照片、联系方式 |
| 线下见面 | 现实中自然可见 | 平台内资料仍不主动展示，是否交换联系方式由用户自己决定 |
| 活动结束后 | 可对同桌成员单独发起 1V1 匿名会话，走正常解锁流程 | — |

**核心原则：平台永远不主动替用户"揭面"。** 线下见到的是现实，平台内的资料解锁仍然由聊天进度决定。

#### 4.5.9 安全与信任机制（线下场景必须有）

| 机制 | 要求 | 优先级 |
| --- | --- | --- |
| 地点限定 | 只能选真实餐厅 POI，禁止私人住址、KTV 包房等封闭场所 | P0 |
| 实名门槛 | 发起线下活动需完成手机号验证；商家需资质审核 | P0（Demo 可 mock） |
| 性别构成透明 | 报名前可见当前性别比例，支持「仅同性别局」选项 | P0 |
| 举报与拉黑 | 活动页、群聊内均有举报入口 | P0 |
| 出席信誉分 | 记录报名后是否到场，多次放鸽子降低信誉分并限制发起权限 | P1 |
| 活动后互评 | 简单的匿名评价（是否愿意再一起吃饭） | P1 |
| 行程分享 / 紧急联系人 | 一键把活动信息分享给亲友 | V2 |

#### 4.5.10 数据模型（给研发参考）

```json
{
  "eventId": "ev_2026_0901",
  "hostType": "user",
  "hostId": "u_10086",
  "title": "聊聊 AI 创业，周五晚",
  "poi": { "poiId": "poi_8899", "name": "XX 餐厅", "address": "…", "lat": 0, "lng": 0 },
  "startAt": "2026-09-04T19:00:00+08:00",
  "signupDeadline": "2026-09-04T17:00:00+08:00",
  "minSize": 3,
  "maxSize": 8,
  "budgetLevel": "100-200",
  "payType": "AA",
  "requiredTags": ["interest_ai", "identity_startup"],
  "genderPolicy": "balanced",
  "signupMode": "review",
  "status": "recruiting",
  "members": [
    { "userId": "u_10086", "role": "host", "matchScore": 100, "checkedIn": false }
  ],
  "merchantBenefit": {
    "type": "discount",
    "value": "0.8",
    "validWindow": "2026-09-04T18:00:00+08:00/2026-09-04T22:00:00+08:00",
    "redeemCode": "RC_2026_0901",
    "redeemed": false
  }
}
```

关键字段说明：
- `hostType`：`user`（C 端）或 `merchant`（B 端），前端据此展示商家标识。
- `requiredTags`：复用 4.1 的 `tagId`，与用户标签同一套词表，否则匹配度算不出来。
- `matchScore`：报名时快照当时的匹配度，避免标签变化导致历史数据漂移。
- `merchantBenefit`：仅 B 端活动有，成团后才生效。

#### 4.5.11 Demo 实现范围（黑客松阶段）

| 做 | 不做 |
| --- | --- |
| C 端发起活动完整表单 + 活动广场 + 匹配度排序 | 商家自助后台（用 mock 商家数据预置 2–3 场 B 端活动即可） |
| 报名、成团、自动取消的状态流转 | 真实支付、AA 分账 |
| B 端活动的展示、券的发放与静态核销码 | 券的真实核销系统对接、POS 打通 |
| 成团后的多人群聊 + 多人破冰工具 | 出席信誉分、活动后互评 |
| 地点用真实 POI（可用地图 API 或预置几个餐厅） | 行程分享、紧急联系人 |

---

## 5. 会议关键决策记录

| # | 议题 | 结论 | 依据 |
| --- | --- | --- | --- |
| 1 | 第三方数据用真实接口还是 mock | 保留真实接口设计，MVP 用 mock 数据跑通链路 | 微信/抖音需本地采集、成本高；4 人团队无法真实采集全量数据 |
| 2 | 抓取的标签要不要给对方看 | 不给。标签仅对用户本人可见（`self_only`） | 匿名是产品立身之本；曾有分歧，最终统一 |
| 3 | 匹配是否展示匹配点明细 | 只展示匹配度百分比，不展示匹配在哪里 | 保留新鲜感与探索感 |
| 4 | 匹配权重由谁定 | **由开发团队在后端按 100% 比率配置，用户不可见、不可调**，不做任何用户端权重配置交互；权重取值后续靠 A/B 测试选优，Demo 直接用默认权重表 | 会上确认"按百分比配、随便调"的思路本质是算法调参而非用户功能；用户端零配置也最符合匿名产品的调性，且大幅省开发量 |
| 5 | 核心场景是聊天还是游戏 | **聊天为核心，游戏为聊天内的破冰工具** | 多人语音游戏房间方案的四点风险（见 4.3.1） |
| 6 | 匹配后如何推进关系 | 按聊天天数/热度阶段性解锁个人信息，终点是交换联系方式 | 对标 Soul/陌陌/探探的成熟机制 |
| 7 | 1 对多 / 线下多人场景 | 会上结论为本期不做，**现已调整为纳入本期**，落地形态是 3–10 人线下兴趣饭局（见 4.5） | 会上的顾虑是"多人在线聊天容易失控"，而饭局是**以线下见面为目标的多人局**，群聊只是成团后的短期承载，规避了原顾虑；同时它是本产品与 1V1 dating 类产品的核心差异点 |
| 8 | 约饭局是否引入 B 端商家 | **引入**，餐厅可自行发起饭局并让利（折扣券等）换客流 | 补齐商业闭环，也解决冷启动阶段活动数量不足的问题 |
| 9 | 线下场景怎么处理匿名机制的冲突 | **平台永不主动揭面**：报名和审核阶段只看匹配度与共同标签，成团群聊内仍是匿名代号 + 兴趣标签；线下见面是现实层面的自然暴露，平台内资料解锁仍由聊天进度决定 | 匿名是产品调性根基，不能因为线下场景就整体退让 |
| 10 | 功能做广还是做深 | 收敛为四个功能点：**真实数据 + 匿名匹配 + 破冰工具 + 线下约饭**，其余列入待办池 | 黑客松周期有限，需保证链路完整可演示 |
| 11 | UI 产出方式 | 用 AI 生成原型/UI 图，再交设计优化 | 团队现状 |

---

## 6. MVP 范围与优先级

### 必做（P0，缺一个 Demo 不成立）

1. 注册流程：8 个自填标签 + 第三方 App 授权入口
2. 第三方数据接入（mock 实现 + 真实接口抽象层）→ 生成 20+ 真实标签
3. 匹配算法：硬性过滤 + 后端权重配置表（统一配置文件，合计 100%）+ 逐标签相似度打分 + 展示平滑。**无用户端权重配置页**
4. 匹配结果页（仅百分比 + 「？」匹配点）
5. 匿名聊天（文字 + 系统卡片消息）
6. 破冰工具箱：摇骰子、抽任务卡、匹配点解锁任务
7. 关系进度条与阶段解锁（至少跑通 L0 → L1 → L4 的演示路径，Demo 可用调试开关加速）
8. 线下约饭：C 端发起表单 + 活动广场（匹配度排序）+ 报名成团状态流转 + 成团群聊
9. 线下约饭：B 端活动展示 + 券的发放 + 静态核销码（商家数据用 mock 预置 2–3 场）

### 可选（P1，时间允许）

- 按「交友目的」区分的差异化权重组
- 语音聊天
- 扑克牌游戏
- 真实 API 打通（至少一个数据源打通即为强加分项）
- 多人破冰工具的完整版（轮流话题接力等）
- 出席信誉分、活动后互评

### 不做（V2 及以后）

- 大富翁（P2，视时间）
- A/B 测试与权重调参（本次 Demo 只用一套固定权重，不做实验分流）
- 微信 / 抖音数据接入
- 商家自助后台、真实券核销系统与 POS 打通
- 真实支付与 AA 分账
- 行程分享 / 紧急联系人
- 多人语音游戏房间

---

## 7. 分工安排

| 模块 / 事项 | 负责人 |
| --- | --- |
|  |  |
|  |  |
|  |  |
|  |  |

---

## 8. 风险与待确认事项

| 风险 / 待确认 | 影响 | 应对建议 | 负责人 |
| --- | --- | --- | --- |
| 多邻国 / Keep 是否有可用开放接口 | 影响"真实性"这一核心卖点的说服力 | 优先验证，实在接不通就用 mock，但 Demo 话术要讲清接口已抽象预留 | 待定 |
| 比赛是否要求提交可上线产品 | 决定做深还是做全 | 尽快向主办方确认；未确认前按"链路完整可演示"标准执行 | 待定 |
| 匹配度分数分布可能过低，观感差 | 用户看到 20% 直接流失 | 做展示层平滑，收敛到 60%–98% 区间 | 待定 |
| 默认权重是团队拍的，未经数据验证 | 匹配质量无从判断 | Demo 阶段接受，权重集中配置便于随时调；上线后再靠 A/B 测试选优 | 待定 |
| 解锁周期（7 天）在 Demo 中无法真实演示 | 评委看不到完整链路 | 提供演示模式，可手动跳阶段 | 待定 |
| 任务卡内容库不足 | 破冰工具显得单薄 | 提前准备 30 条以上分类话题 | 待定 |
| 匿名 + 真实数据可能带来隐私合规问题 | 上线风险 | 明确授权弹窗、数据最小化采集、标签仅本人可见 | 待定 |
| 线下饭局的人身安全风险 | 一旦出事对产品是致命打击 | 强制真实餐厅 POI、实名门槛、性别构成透明、举报入口、支持仅同性别局（见 4.5.9） | 待定 |
| 约饭冷启动：活动数量不足导致广场空 | 用户进来没有局可报 | B 端商家活动兜底 + Demo 阶段预置若干 mock 活动；上线初期限定 1–2 个城市 | 待定 |
| 成团失败率高（放鸽子、凑不满人） | 用户体验挫伤 | 成团下限设为 3 人、报名截止提前 2 小时、未成团自动取消并通知；P1 补出席信誉分 | 待定 |
| 匿名机制与线下见面的体验冲突 | 用户困惑，产品逻辑不自洽 | 已在 4.5.8 定义分阶段可见度规则，研发不要自行推断 | 待定 |
| B 端让利意愿与结算方式未验证 | 影响商业模式成立 | Demo 阶段用 mock 商家；赛后需实地访谈 3–5 家餐厅验证让利幅度 | 待定 |

---

## 9. 后续规划（V2 展望）

1. **约饭局的商业化闭环**：商家自助后台、券核销与 POS 打通、按成团数或核销数向商家收费、活动置顶推广位。
2. **更多线下活动类型**：从吃饭扩展到咖啡局、运动局（可直接复用 Keep 标签）、观影局、City Walk。
3. **更多数据源**：微信、抖音、Spotify、Steam、GitHub 等，扩大真实标签维度。
3. **AI 陪练/记录**：会上提到的设想 —— 在垂直搭子场景（如语言学习）中开语音做一段对话，由 AI 记录并生成能力标签，形成数据闭环。
4. **奖励与成长体系**：匹配点全解锁后的称号、皮肤、稀有匹配机会等。
5. **权重调参与 A/B 测试**：搭建实验分流能力，用开聊率、平均聊天天数、解锁阶段推进率等指标验证不同权重组合的效果，逐步替换默认权重。
